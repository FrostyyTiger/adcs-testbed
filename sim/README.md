# Simulation

Python models of the testbed dynamics (rigid body + reaction wheels), used for control design, gain tuning, and estimator development before running on hardware.

Planned contents:

- Plant model (inertia, wheel dynamics, friction, sensor noise)
- PID / controller tuning notebooks
- Attitude estimation (complementary filter → Kalman filter) prototyping
